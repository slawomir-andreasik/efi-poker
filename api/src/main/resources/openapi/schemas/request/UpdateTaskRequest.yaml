type: object
properties:
  title:
    type: string
    minLength: 1
    maxLength: 255
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  description:
    type: string
    maxLength: 2000
    pattern: '^(?![\s\S]*<[a-zA-Z/])[\s\S]*$'
  sortOrder:
    type: integer
