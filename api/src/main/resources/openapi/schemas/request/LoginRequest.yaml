type: object
required: [username, password]
properties:
  username:
    type: string
    minLength: 3
    maxLength: 100
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  password:
    type: string
    minLength: 8
    maxLength: 128
  rememberMe:
    type: boolean
    default: false
