type: object
required: [title]
properties:
  title:
    type: string
    minLength: 1
    maxLength: 255
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  description:
    type: string
    maxLength: 2000
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  sortOrder:
    type: integer
    default: 0
