type: object
properties:
  title:
    type: string
    minLength: 1
    maxLength: 255
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  description:
    type: string
    maxLength: 2000
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  deadline:
    type: string
    format: date-time
  topic:
    type: string
    maxLength: 500
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  commentTemplate:
    type: string
    maxLength: 2000
    pattern: '^(?!.*<[a-zA-Z/]).*$'
  commentRequired:
    type: boolean
  autoRevealOnDeadline:
    type: boolean
