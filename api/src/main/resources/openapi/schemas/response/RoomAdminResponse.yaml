allOf:
  - $ref: './RoomBase.yaml'
  - type: object
    required:
      - tasks
      - participants
    properties:
      tasks:
        type: array
        items:
          $ref: './TaskWithAllEstimatesResponse.yaml'
      participants:
        type: array
        items:
          $ref: './ParticipantResponse.yaml'
