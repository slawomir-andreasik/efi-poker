allOf:
  - $ref: './TaskBase.yaml'
  - type: object
    required:
      - roomId
      - createdAt
    properties:
      roomId:
        type: string
        format: uuid
      createdAt:
        type: string
        format: date-time
