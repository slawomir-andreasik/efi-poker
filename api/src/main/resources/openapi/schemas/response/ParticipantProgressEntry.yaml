type: object
required:
  - nickname
  - votedCount
  - totalTasks
properties:
  nickname:
    type: string
  votedCount:
    type: integer
  totalTasks:
    type: integer
  hasCommentedAll:
    type: boolean
    description: true if participant left comment on every voted task
