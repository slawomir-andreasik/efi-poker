allOf:
  - $ref: './RoomBase.yaml'
  - type: object
    required:
      - projectId
      - createdAt
    properties:
      projectId:
        type: string
        format: uuid
      createdAt:
        type: string
        format: date-time
