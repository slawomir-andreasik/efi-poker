services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: efipoker
      POSTGRES_USER: efipoker
      POSTGRES_PASSWORD: changeme
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U efipoker"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
